
The argot directory provides details of the common data
types and meta dictionary.  The .argot files are text files
which can be edited and compiled using the argot dictionary
compiler.  The compiler produces .dictionary files which can
be read directly by Argot.

